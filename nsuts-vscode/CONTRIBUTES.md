## Разработка

### Первый запуск

```bash
nvm use
npm ci
```

Нажмите `F5` для запуска в режиме отладки.

### Команды

- `npm run compile` — скомпилировать проект
- `npm run watch` — компиляция при изменениях
- `npm run codegen` — обновить типы API
- `npm run pack` — собрать .vsix файл
- `npm run test` - прогнать тесты

## Структура проекта

```
src/
├── api/          # API клиент и типы
├── commands/     # все команды расширения
├── repositories/ # работа с данными
├── statusBar/    # элементы статус-бара
├── views/        # view-компоненты
└── extension.ts  # точка входа
```

## Внесение изменений

1. Форкните репозиторий
2. Создайте ветку: `feature/название` или `fix/название`
3. Сделайте изменения
4. Убедитесь что компилируется: `npm run compile`
5. Создайте Pull Request

## Сообщения об ошибках

Создайте Issue с описанием:

- Что случилось
- Как повторить
- Ожидаемый результат
